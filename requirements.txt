# Re-Imaging Price Trends - Requirements
# Essential packages only for streamlined installation

# Core ML/DL frameworks 
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0

# Data processing
pyarrow>=14.0.0
fastparquet>=2023.10.0
feather-format>=0.4.1

# Visualization
matplotlib>=3.7.0

# Progress bars and utilities
tqdm>=4.65.0
joblib>=1.3.0

# NOTE: Colab already includes many packages - install only essentials
# Removed: h5py, tables (no longer using HDF5)
# Removed: seaborn, scipy, scikit-learn, numba (not actively used)
# Removed: wrds, psycopg2-binary, sqlalchemy (data generation complete)
# Removed: psutil (only used in notebooks for memory check)