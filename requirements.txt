# Re-Imaging Price Trends - Requirements
# Colab-safe versions (excludes problematic packages)

# Core ML/DL frameworks 
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0

# Data processing and storage
pyarrow>=14.0.0
fastparquet>=2023.10.0
h5py>=3.8.0
tables>=3.8.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars and utilities
tqdm>=4.65.0
joblib>=1.3.0

# Scientific computing
scipy>=1.10.0
scikit-learn>=1.3.0

# Performance optimization
numba>=0.58.0

# Data access (WRDS integration)
wrds>=3.4.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# NOTE: Colab에서는 ipython, jupyter, notebook 설치 금지 (충돌 발생)
# 이미 Colab에 설치되어 있으므로 별도 설치 불필요